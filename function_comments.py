import dictionary_lambda
def function_commments_test(agument):
    '''
    This is a test fot comments in function

    #In the meantime#

    parameters
    -
    z : redshfit
        array
    dTb : T21 temperature
        unit mK
    Ts : spin temperature
        in mK

    I want to test import modules.

    ##what is the effect?##

    Global 21-cm signature relative to cosmic microwave background in mK.

    Parameters
    ----------
    z : float, np.ndarray
        Redshift
    xavg : float, np.ndarray
        Volume-averaged ionized fraction, i.e., a weighted average
        between the volume of fully ionized gas and the semi-neutral
        bulk IGM beyond.
    Ts : float, np.ndarray
        Spin temperature of intergalactic hydrogen.

    Returns
    -------
    Differential brightness temperature in milli-Kelvin.

    '''

def func(path, field_storage, temporary):
    '''基本描述

    详细描述

    :param path: The path of the file to wrap
    :type path: str
    :param field_storage: The :class:`FileStorage` instance to wrap
    :type field_storage: FileStorage
    :param temporary: Whether or not to delete the file when the File instance is destructed
    :type temporary: bool
    :returns: A buffered writable file descriptor
    :rtype: BufferedFileStorage
    '''
    pass

def func(path, field_storage, temporary):
    '''基本描述

    详细描述

    Parameters
    ----------
    path : str
        The path of the file to wrap
    field_storage : FileStorage
        The :class:`FileStorage` instance to wrap
    temporary : bool
        Whether or not to delete the file when the File instance is destructed

    Returns
    -------
    BufferedFileStorage
        A buffered writable file descriptor
    '''
    pass

def func(path, field_storage, temporary):
    '''基本描述

    详细描述

    Args:
        path (str): The path of the file to wrap
        field_storage (FileStorage): The :class:`FileStorage` instance to wrap
        temporary (bool): Whether or not to delete the file when the File instance is destructed

    Returns:
        BufferedFileStorage: A buffered writable file descriptor
    '''
    pass

dictionary_lambda.dictionaryLambda()